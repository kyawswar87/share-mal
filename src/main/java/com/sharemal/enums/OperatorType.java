package com.sharemal.enums;

/**
 * Enum representing the type of bill splitting operator
 */
public enum OperatorType {
    /**
     * Split the bill equally among all participants
     */
    EQUALLY,
    
    /**
     * Split the bill with custom amounts for each participant
     */
    CUSTOM
}
